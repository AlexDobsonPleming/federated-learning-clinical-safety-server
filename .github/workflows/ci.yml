name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  backend:
    name: 🐍 Django Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - run: python manage.py migrate --noinput
      - run: pytest --maxfail=1 --disable-warnings -q

  frontend:
    name: 🖥️ Client Tests
    runs-on: ubuntu-latest
    needs: backend

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'   # ← pin to Node 18 instead of .nvmrc
          cache: 'yarn'

      - run: |
          cd client
          yarn install --frozen-lockfile

      - run: |
          cd client
          npm run build

      - run: |
          cd client
          npm test -- --watchAll=false
